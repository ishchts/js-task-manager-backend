extends ../layouts/application.pug

block header
  div= t('views.label.title')
  a.btn.btn-primary.btn-lg(href="/labels/new")= t('views.label.btn.create')

block content
  table.table.table-striped
    thead
      tr
        th= t('views.label.id')
        th= t('views.label.name')
        th= t('views.label.createdAt')
    tbody
      each label in labels
        tr
          td= label.id
          td= label.name
          td= formatDate(label.createdAt)
          td
            div.d-flex.flex-wrap
              a.btn.btn-primary.me-1(href=`/labels/${label.id}/edit`)= t('views.label.btn.edit')
              +buttonTo(`/labels/${label.id}/delete`, 'delete')(class="btn btn-danger" value=t('views.label.btn.delete'))